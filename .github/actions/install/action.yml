runs:
  using: "composite"
  steps:
    - run: corepack enable
      shell: bash
    - uses: actions/setup-node@v5
      with:
        node-version-file: ".node-version"
        cache: "pnpm"
    - run: pnpm install
      shell: bash
